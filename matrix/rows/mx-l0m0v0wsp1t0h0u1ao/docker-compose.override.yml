# Matrix row: mx-l0m0v0wsp1t0h0u1ao (generated) — GENERATED, DO NOT EDIT.
# Source of truth: matrix/config-model.mjs; regenerate with `make matrix-generate`.
# local_idp=off mfa=off verification=off webauthn=sequencing providers=1 tenant_service=absent hook_service=absent user_verification=present access_token=opaque

services:
  kratos:
    environment:
      - SELFSERVICE_METHODS_PASSWORD_ENABLED=false
      - SELFSERVICE_METHODS_PROFILE_ENABLED=false
      - SELFSERVICE_METHODS_CODE_ENABLED=false
      - SELFSERVICE_METHODS_TOTP_ENABLED=false
      - SELFSERVICE_METHODS_LOOKUP_SECRET_ENABLED=true
      - SELFSERVICE_METHODS_WEBAUTHN_ENABLED=true
      - SELFSERVICE_METHODS_WEBAUTHN_CONFIG_PASSWORDLESS=false
      - SELFSERVICE_FLOWS_RECOVERY_ENABLED=false
      - SELFSERVICE_FLOWS_VERIFICATION_ENABLED=false
      - SESSION_WHOAMI_REQUIRED_AAL=highest_available
    command: exec kratos serve -c /etc/config/kratos/kratos.yml -c /etc/config/kratos/kratos.dex.yml --dev --watch-courier

  login-ui:
    environment:
      - MFA_ENABLED=false
      - OIDC_WEBAUTHN_SEQUENCING_ENABLED=true
      - VERIFICATION_ENABLED=false

  hydra:
    environment:
      - STRATEGIES_ACCESS_TOKEN=opaque

  tenant-service:
    scale: 0

  hook-service:
    scale: 0

