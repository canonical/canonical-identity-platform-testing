# Matrix row: mx-l1m0v1wsp2t1h0u0ao (generated) — GENERATED, DO NOT EDIT.
# Source of truth: matrix/config-model.mjs; regenerate with `make matrix-generate`.
# local_idp=on mfa=off verification=on webauthn=sequencing providers=2 tenant_service=present hook_service=absent user_verification=absent access_token=opaque

services:
  kratos:
    environment:
      - SELFSERVICE_METHODS_PASSWORD_ENABLED=true
      - SELFSERVICE_METHODS_PROFILE_ENABLED=true
      - SELFSERVICE_METHODS_CODE_ENABLED=true
      - SELFSERVICE_METHODS_TOTP_ENABLED=false
      - SELFSERVICE_METHODS_LOOKUP_SECRET_ENABLED=true
      - SELFSERVICE_METHODS_WEBAUTHN_ENABLED=true
      - SELFSERVICE_METHODS_WEBAUTHN_CONFIG_PASSWORDLESS=false
      - SELFSERVICE_FLOWS_RECOVERY_ENABLED=true
      - SELFSERVICE_FLOWS_VERIFICATION_ENABLED=true
      - SESSION_WHOAMI_REQUIRED_AAL=highest_available
    command: exec kratos serve -c /etc/config/kratos/kratos.yml -c /etc/config/kratos/kratos.google.yml --dev --watch-courier

  login-ui:
    environment:
      - MFA_ENABLED=false
      - OIDC_WEBAUTHN_SEQUENCING_ENABLED=true
      - VERIFICATION_ENABLED=true
      - MULTI_TENANCY_ENABLED=true
      - TENANT_SERVICE_GRPC_ADDRESS=tenant-service:50051

  hydra:
    environment:
      - STRATEGIES_ACCESS_TOKEN=opaque

  hook-service:
    scale: 0

  user-verification-service:
    scale: 0

